import istar from '../istar'

//Temporary Workaround to the setup Events change...
//For now this will avoid loading the default event functions.
let setup_events = function setup_events(graph){
    //console.log('istar :', istar);
}

export default setup_events