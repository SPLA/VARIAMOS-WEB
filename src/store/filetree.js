const state = {
    data: []
}

const getters = {
    getdata: state => {
        return state.data
    }
}

const actions = {
    
}

const mutations = {
    
}

export default {
    state,
    getters,
    actions,
    mutations
}